package br.com.abc.introducao;

public class Anotaçoes {
    //Quando nos temos associaçoes temos o relacionamenteo "tem um".
    //Quando nos temos herança temos o relacionamento "é um".
    /*A Java Virtual Machine(JVM) fica uma camada acima dos sistemas operacionais e serve como interpretador
     da linguagem, ou seja, a JVM é um programa que consegue entender a linguagem compilada do Java que
     será chamada de bytecode. Todas as vezes que criarmos um programa em Java, esse programa terá a extensao
      .java, apos a compilaçao é gerado um bytecode com a extensao .class. */
    //A divisao de 2 numeros inteiros SEMPRE gera um numero inteiro.
    /*Quando concatenamos uma String com um numero, o proprio java faz a concatenaçao e transforma(por
    debaixo dos panos) o numero em String tmb. */
    /*Diferenças entre um codigo compilado e um condigo interpretado:
    COMPILADOR:
    O codigo compilado gasta mais memoria,pois guarda o codigo todo da memoria do computador.
    INTERPRETADOR:
    O codigo interpretado ocupa menos espaço, pois o codigo é executado linha por linha, sendo que apos
    a linha ser executada ele é apagada da memoria.
    */

    //Primeira letra maiuscula significa ou classe ou interface.
    //O java.lang ja vem carregado por padrao em todo sistema java.
}
